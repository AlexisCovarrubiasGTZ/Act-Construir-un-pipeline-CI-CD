{
  "name": "mi-app-cicd",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "build": "mkdir -p dist && cp src/index.js dist/index.js"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}