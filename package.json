{
  "name": "mi-app-cicd",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "build": "echo Construyendo app... && (if not exist dist mkdir dist) && copy src\\index.js dist\\index.js"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}